# M1 Brief — 单序列前向：模型能出字

> L 层覆盖：**L1 Model 主战场** + 7 Protocol 骨架首套（L2/L3/L4 均放最小占位实现）
>
> 目标：用一个真实 LLM（Qwen3-0.6B）在 Mac MPS 上跑通一次贪心解码，把 4 层抽象的接口形态钉死。

---

## 1. 完成定义（DoD）

全部满足才算 M1 完成：

1. `python -m inferlite.cli "你好" --max-new-tokens 32` 能稳定吐出连续中文/英文序列（不是乱码）
2. 7 个 Protocol 文件全部存在、签名稳定（即便实现是 trivial）
3. `EngineCore.step()` 走完三段式：`scheduler.schedule → executor.execute → postprocess`，CLI 通过 `LLMModel` 接口调用，**不**直接 import 具体模型类
4. L0 单元测试 8 项全绿（见 §5）
5. L1 整模型 logits vs HuggingFace `transformers` allclose（atol=1e-3, rtol=1e-3）通过
6. L2 e2e 测试：固定 prompt + 固定 seed，前 16 个 token 与 HF `model.generate(do_sample=False)` 完全一致
7. 知乎文章草稿：《手写 LLM 推理引擎 1：先让模型出字》初稿（500–1500 字，含至少一张架构图）
8. PROGRESS.md 更新，Tag `v0.1`

**显式不做**（留给后续 M）：KV cache（M2）、batching（M3）、PagedAttention（M4）、sampler 多策略（M5）、benchmark（M5）。

---

## 2. 必读资料（动手前 1 天读完）

### 2.1 主线（按顺序）

| # | 资料 | 重点章节 / 文件 | 用途 | 预估时间 |
| --- | --- | --- | --- | --- |
| 1 | `GeeeekExplorer/nano-vllm`（GitHub） | `nanovllm/models/qwen3.py`（约 200 行） | 看千行版 Qwen3 的目标体量 | 30 min |
| 2 | `huggingface/transformers` | `src/transformers/models/qwen3/modeling_qwen3.py` | **L1 数值对齐唯一源** — 每一层算子要和它逐张量 allclose | 1 h |
| 3 | Sebastian Raschka《Qwen3 from scratch》notebook | 全文 | 教学最清晰，公式 → 代码逐句对照 | 2 h |
| 4 | Qwen3 技术报告 arXiv:2505.09388 | §3 Architecture | 确认 GQA 头数 / RoPE base / tie_embedding / QK-norm 等超参 | 30 min |

### 2.2 设计参照（写 Protocol 骨架时翻）

| 资料 | 看什么 | 不看什么 |
| --- | --- | --- |
| `vllm/v1/engine/core.py` 的 `step()` | 三段式不变量 | TP/PP / spec decoding 的实现细节 |
| `vllm/model_executor/models/registry.py` | Registry 写法 | lazy import 复杂逻辑 |
| `vllm/model_executor/models/qwen3.py` | 工程级对照（含 paged）— **只读不抄** | paged attention / TP 切分 |

### 2.3 概念背景（卡壳时再翻）

- **GQA**：Ainslie 2023《GQA》§2
- **RoPE**：Su 2021《RoFormer》§3.4.2（旋转的复数实现）
- **RMSNorm**：Zhang & Sennrich 2019

---

## 3. 文件骨架建议

```
inferlite/
├── pyproject.toml
├── inferlite/
│   ├── __init__.py
│   ├── cli.py                          # 极简 CLI 入口
│   ├── config.py                       # ModelConfig dataclass
│   │
│   ├── model/                          # L1
│   │   ├── __init__.py
│   │   ├── protocol.py                 # LLMModel Protocol（M1 钉死）
│   │   ├── registry.py                 # Registry（M1 只注册一个 Qwen3）
│   │   ├── weight_map.py               # WeightMap 抽象（M1 仅 Qwen3 实现）
│   │   ├── layers.py                   # RMSNorm / RotaryEmbedding / SDPAAttention / SwiGLU
│   │   └── qwen3.py                    # Qwen3Model（继承 LLMModel）
│   │
│   ├── cache/                          # L2
│   │   ├── __init__.py
│   │   ├── protocol.py                 # KVCache Protocol（M1 钉死，M2 升级）
│   │   └── trivial.py                  # NoOpKVCache —— M1 不存 KV，每步 full forward
│   │
│   ├── scheduler/                      # L3
│   │   ├── __init__.py
│   │   ├── protocol.py                 # Scheduler Protocol
│   │   └── trivial.py                  # SingleRequestScheduler —— 只跑一个请求
│   │
│   ├── sampler/                        # L3 一部分
│   │   ├── __init__.py
│   │   ├── protocol.py                 # Sampler Protocol
│   │   └── greedy.py                   # GreedySampler
│   │
│   ├── executor/                       # 串联 L1+L2+L3
│   │   ├── __init__.py
│   │   ├── protocol.py                 # ModelExecutor Protocol
│   │   └── uniproc.py                  # UniprocExecutor —— 单进程版
│   │
│   ├── engine/                         # 三段式 step()
│   │   ├── __init__.py
│   │   └── core.py                     # EngineCore.step() 钉死不变量
│   │
│   └── server/                         # L4（M5 才真正用 FastAPI；M1 留空 __init__）
│       └── __init__.py
│
└── tests/
    ├── unit/                           # L0
    │   ├── test_rmsnorm.py
    │   ├── test_rope.py
    │   ├── test_attention.py
    │   ├── test_swiglu.py
    │   └── test_weight_map.py
    ├── module/                         # L1
    │   └── test_qwen3_logits.py        # vs transformers allclose
    ├── e2e/                            # L2
    │   └── test_greedy_decode.py       # vs HF generate(do_sample=False)
    └── invariant/                      # L3
        └── test_step_three_phase.py    # step() 必走三段式
```

> 共 **17 个 .py 文件 + 9 个 test 文件 ≈ 1500 行**。M1 全程目标体量。

---

## 4. Protocol 签名（M1 必须钉死）

```python
# inferlite/model/protocol.py
class LLMModel(Protocol):
    config: ModelConfig
    def forward(
        self,
        input_ids: Optional[Tensor] = None,
        inputs_embeds: Optional[Tensor] = None,   # M13 VLM 预留
        positions: Tensor = ...,
        kv_cache: "KVCache" = ...,
    ) -> Tensor: ...                              # 返回 logits [B, T, V]
    @classmethod
    def load_from_hf(cls, hf_path: str) -> "LLMModel": ...

# inferlite/cache/protocol.py
class KVCache(Protocol):
    def append(self, layer_id: int, k: Tensor, v: Tensor, slot: Any) -> None: ...
    def get(self, layer_id: int, slot: Any) -> tuple[Tensor, Tensor]: ...
    def free(self, slot: Any) -> None: ...

# inferlite/scheduler/protocol.py
class Scheduler(Protocol):
    def schedule(self, requests: list["Request"]) -> "Batch": ...
    def update(self, output: "ModelOutput") -> list["Request"]: ...

# inferlite/sampler/protocol.py
class Sampler(Protocol):
    def __call__(self, logits: Tensor, params: "SamplingParams") -> Tensor: ...

# inferlite/executor/protocol.py
class ModelExecutor(Protocol):
    def execute_model(self, batch: "Batch") -> "ModelOutput": ...

# inferlite/engine/core.py
class EngineCore:
    def step(self) -> list["Request"]:
        batch  = self.scheduler.schedule(self.requests)        # phase A
        output = self.executor.execute_model(batch)            # phase B
        return self.scheduler.update(output)                   # phase C

# inferlite/scheduler/protocol.py（Drafter 留位，M7 才实现）
class Drafter(Protocol):
    def propose(self, request: "Request") -> list[int]: ...
```

**纪律**：M1 即便有些 Protocol 只有一个 trivial 实现，文件也必须存在。M2 开始所有升级**只加新实现**，不改签名。

---

## 5. L0 单元测试清单（8 项）

每个测试 ≤ 30 行，全部以 **transformers 同名函数** 为 ground truth：

| # | 文件 | 测试对象 | Ground truth | 容差 |
| --- | --- | --- | --- | --- |
| 1 | `test_rmsnorm.py` | `inferlite.model.layers.RMSNorm` | `transformers.models.qwen3.modeling_qwen3.Qwen3RMSNorm` | 1e-5 |
| 2 | `test_rope.py::test_apply_rotary` | apply_rotary 输出 | transformers `apply_rotary_pos_emb` | 1e-5 |
| 3 | `test_rope.py::test_inv_freq` | RoPE inv_freq buffer | 直接对照 `1.0 / (base ** (torch.arange(0, d, 2) / d))` | 1e-7 |
| 4 | `test_attention.py::test_qk_norm` | Q/K 经 QK-norm 后的均值/方差 | transformers Qwen3Attention | 1e-5 |
| 5 | `test_attention.py::test_gqa_repeat_kv` | KV 头复制 | `transformers.models.llama.modeling_llama.repeat_kv` | exact eq |
| 6 | `test_attention.py::test_full_block` | 单层 Attention 输出 | transformers Qwen3Attention | 1e-4 |
| 7 | `test_swiglu.py` | MLP（SwiGLU）输出 | transformers Qwen3MLP | 1e-5 |
| 8 | `test_weight_map.py` | HF safetensors → 自家命名 | 抽 5 个权重对比形状 + sum | exact eq |

L1 / L2 / L3 各 1 个测试，详见 §3 文件骨架。

---

## 6. 易踩坑预警（按概率排序）

| # | 坑 | 原因 | 防御措施 |
| --- | --- | --- | --- |
| 1 | **logits 偏差大但形状对** | RoPE 实现选错（GPT-J 风格 vs LLaMA 风格 vs Qwen 内部 fused 风格） | 严格对照 transformers `Qwen3RotaryEmbedding`，**不要**自己脑补复数版 |
| 2 | **QK-norm 漏写** | Qwen3 在 Q/K 投影后多了一次 RMSNorm（区别于 Qwen2） | `attention.py` 加 `q_norm` / `k_norm`；测试 4 专门盯这个 |
| 3 | **GQA repeat_kv 顺序错** | KV 头复制要 `repeat_interleave(dim=1)` 不是 `repeat` | 用 transformers 的 `repeat_kv` 直接对照 |
| 4 | **tie_word_embeddings 漏接** | Qwen3-0.6B 的 lm_head 与 embed_tokens 共享权重 | `weight_map.py` 显式处理；测试 8 验证 |
| 5 | **MPS dtype 报错** | MPS 在 fp16 某些算子有坑 | M1 全程 fp32（M5 benchmark 才上 fp16/bf16） |
| 6 | **causal mask 没传** | SDPA 必须 `is_causal=True` | 单测在 attention 测试里盖一个 prompt-len > 1 的 case |
| 7 | **HF 权重路径硬编码** | 不同人本地 HF cache 位置不同 | `load_from_hf("Qwen/Qwen3-0.6B")` 走 `huggingface_hub.snapshot_download` |
| 8 | **首次 forward 慢** | MPS JIT warm-up | benchmark 第一次扔掉，从第二次开始计时（M5 才 benchmark，M1 不管） |

---

## 7. 推进节奏（仅供参考，不强制时间）

```
Day 1   读必读 §2.1 全部 + §2.2，跑通 transformers 官方示例
Day 2   写 config.py + layers.py（RMSNorm/SwiGLU 先），过 L0 测试 1, 7
Day 3   写 RoPE + Attention，过 L0 测试 2, 3, 4, 5, 6
Day 4   写 weight_map.py + qwen3.py 整模型拼装，过 L0 测试 8 + L1 logits allclose
Day 5   写 7 Protocol 骨架 + EngineCore.step() + GreedySampler + cli.py，过 L2 e2e + L3 invariant
Day 6   修 bug、补注释、写文章草稿、push tag v0.1
```

---

## 8. 启动 checklist

写第一行代码前确认：

- [ ] `inferlite` 目录已 clone 到本地
- [ ] `Qwen/Qwen3-0.6B` 已通过 `transformers.AutoModelForCausalLM.from_pretrained` 跑通过一次
- [ ] 必读 §2.1 第 2、3、4 项已读完
- [ ] 7 个 Protocol 签名（§4）已抄到 README 边上随时查
- [ ] L0 单测 8 项清单（§5）已心中有数
- [ ] 易踩坑（§6）已浏览一遍

全勾才动手。

---

## 9. 概念速查（M1 期间常翻）

> 配合 `scripts/preflight.py` 看，preflight 的 5 行代码就是"transformers 推理最小闭环"。

### 9.1 五个核心对象

| 对象 | 作用 | M1 是否要自己写 |
| --- | --- | --- |
| `AutoTokenizer.from_pretrained(id)` | 字符串 → token id 列表（BPE 分词） | 否，永远复用 transformers |
| `AutoModelForCausalLM.from_pretrained(id, dtype, device_map)` | 下载权重 + 实例化网络 | **是**，inferlite 版叫 `LLMModel.load_from_hf("Qwen/Qwen3-0.6B")` |
| `model.eval()` | 切推理模式（关 dropout / batchnorm 更新） | 是，`__init__` 末尾自动调 |
| `tokenizer.decode(ids, skip_special_tokens=True)` | token id → 字符串 | 否 |
| `model.generate(...)` | 自动循环 forward 直到 max_new_tokens 或 EOS | **要手撕**：cli.py 里自己写循环 + 采样 |

`generate(...)` 的本质（M1 你 cli 要重写的）：

```python
while not done:
    out  = model(input_ids)                 # forward 一次
    next = sampler(out.logits[:, -1, :])    # 取最后位置 logits → 采样
    input_ids = torch.cat([input_ids, next], dim=1)
```

### 9.2 关键超参

**generate 超参**：

| 超参 | 含义 |
| --- | --- |
| `max_new_tokens` | 新生成 token 上限（不含 prompt） |
| `do_sample=False` | 贪心解码（M1 唯一支持的策略） |
| `do_sample=True` + `temperature/top_p/top_k` | 随机采样（M5 才做） |
| `num_beams` | beam search（inferlite **不做**） |
| `pad_token_id` | padding 用，单 prompt 单 batch 不需要 |

**from_pretrained 超参**：

| 超参 | 含义 | M1 用值 |
| --- | --- | --- |
| `<owner>/<name>` repo id | HF Hub 仓库 | `"Qwen/Qwen3-0.6B"` |
| `dtype` | 权重精度 | `torch.float32`（MPS 全程 fp32，§6 第 5 坑） |
| `device_map` | 权重搬到哪 | `"mps"` / `"cuda"` / `"cpu"`（自动选见 preflight.py `pick_device`） |

> transformers 5.x 用 `dtype=`；4.x 老教程是 `torch_dtype=`，二者等价。

**张量转换两件事**：

```python
inputs = tokenizer("你好", return_tensors="pt").to(device)
#                                  ^^^                ^^^
#                                  返回 PyTorch tensor   搬到模型所在设备
```

### 9.3 推理优化常用上下文

```python
with torch.no_grad():       # 不算梯度，省 ~50% 显存 + 加速 5–30%
    out = model(...)

# 等价（更彻底）：
with torch.inference_mode():
    out = model(...)
```

M1 推理路径**必须**包一层，否则 MPS 上显存翻倍。

### 9.4 形状速查（背下来）

| 张量 | 形状 | 含义 |
| --- | --- | --- |
| `input_ids` | `[B, T]` | int64 token ID |
| `inputs_embeds` (M13) | `[B, T, H]` | 跳过 embed 直接喂浮点 |
| 隐藏态 | `[B, T, H]` | 每层 attention/MLP 之间 |
| logits | `[B, T, V]` | 每个位置在词表上的"分数" |
| 下一 token | `[B, 1]` | argmax 出来的 ID |

**字母含义**：

- **B** = batch size（M1 = 1；M3 引入连续 batching）
- **T** = sequence length（token 数）
- **H** = hidden_size（Qwen3-0.6B = 1024）
- **V** = vocab_size（Qwen3 = 151,936）
- **N** = layers（Qwen3-0.6B = 28）

**Qwen3-0.6B 全部超参**（写 `ModelConfig` 时抄这些）：

| 字段 | 值 | 来源 |
| --- | --- | --- |
| hidden_size (H) | 1024 | config.json |
| num_hidden_layers (N) | 28 | |
| num_attention_heads | 16 | |
| num_key_value_heads | 8 | GQA 比例 16:8 |
| head_dim | 128 | hidden_size / num_attention_heads |
| intermediate_size | 3072 | MLP 中间维度 |
| vocab_size (V) | 151936 | tokenizer 表大小 |
| max_position_embeddings | 40960 | 原生上下文（YaRN 后到 1M） |
| rope_theta | 1000000.0 | RoPE 基频 |
| rms_norm_eps | 1e-6 | RMSNorm ε |
| tie_word_embeddings | true | lm_head 与 embed_tokens 共享权重 |

写 `ModelConfig` 时把这 11 个字段塞进去就够了，其他 `transformers.Qwen3Config` 里的字段 M1 都不需要。

### 9.5 一句话贯穿前向

```
[B, T] int → embedding → [B, T, H] →
  ┌────────────── 28 × Transformer Block ──────────────┐
  │  RMSNorm → Attention(GQA + RoPE + QK-norm) + 残差  │
  │  RMSNorm → SwiGLU MLP                     + 残差  │
  └────────────────────────────────────────────────────┘
→ Final RMSNorm → lm_head([V, H] = embed_tokens.T) → [B, T, V]
```

> M1 你要从 RMSNorm 开始一个一个手敲，每敲一个加一个 L0 测试 vs transformers 同名实现 allclose。
